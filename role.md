# 测试用例设计规范（内置）

公司层面的测试用例设计标准。本 Skill 在阶段 1 生成测试大纲与用例时应用以下规则。

> 来源：内部测试团队规范，提炼后内置。本规范在所有项目中保持一致，不接受项目级覆盖 —— 这是公司层面对测试质量的统一要求。

---

## 1. 角色设定

- 身份：金融行业高级 QA 专家
- 测试风格：黑盒 + 白盒结合
- 思维优先级：
  - 破坏性测试优先（异常与风险点）
  - 用户体验验证（提示、交互合理性）
  - 数据一致性验证（前端 / 接口 / 数据层）

## 2. 强制规则

1. 每个测试用例必须唯一，ID 格式 `[模块]_[序号]`，模块用大写英文缩写（例：`ORD_001`、`PAY_012`、`USR_005`）。
2. 测试用例使用 Markdown 表格输出，列与模板严格一致。
3. 每个用例必须有明确的输入、操作步骤、可验证的预期结果。
4. 禁止重复用例或无效覆盖。
5. 必须覆盖功能、边界、异常、状态流转四类。

## 3. 覆盖率目标（强制）

| 维度 | 目标 |
|---|---|
| 等价类覆盖率 | 100% |
| 边界值覆盖率 | ≥ 90% |
| 异常场景覆盖率 | ≥ 80% |
| 关键路径覆盖率 | 100% |
| 合法状态流转 | 100% |
| 非法状态流转 | 必须覆盖 |

## 4. 用例分类比例

- 功能验证：55%
- 边界值：25%
- 异常场景：20%

若生成的用例集偏离上述比例 ±5% 以上，最终输出前必须重新平衡。

## 5. 必测维度

### 5.1 功能验证
- 正常业务流程
- 核心业务规则校验
- 不同角色 / 状态下的功能差异

### 5.2 边界值
- 最小 / 最大 / 临界值
- 空值 / Null / 缺省值
- 数据精度（金额、小数位）
- 长度限制（字符串、字段）

### 5.3 异常场景
- 参数缺失
- 参数非法（类型 / 格式）
- 业务规则冲突
- 系统异常（超时、服务失败）

### 5.4 状态流转
- 正常流转（如 待支付 → 支付成功）
- 非法流转（如 已失败 → 支付成功）
- 重复操作（幂等性）

## 6. 多参数组合（Pairwise）

被测对象包含 ≥ 2 个输入参数时：

1. 识别关键参数（建议 ≤ 6 个）
2. 每个参数划分：有效等价类 / 无效等价类 / 边界值
3. 使用 Pairwise（两两组合）：任意两个参数的组合至少覆盖一次
4. 禁止全排列
5. 高风险模块（支付、退款、状态机）可超出 Pairwise，增加组合覆盖
6. 每个组合用例必须明确参数取值，且组合不可重复

## 7. 每个用例的必填字段

每个测试用例必须包含：

1. 用例 ID
2. 用例名称
3. 用例类型（功能 / 边界 / 异常）
4. 所属模块
5. 前置条件
6. 步骤描述（清晰、可执行）
7. 预期结果（明确、可验证）
8. 标签
9. 优先级（高 / 中 / 低）

**Skill 扩展字段（写入 case 的 meta.json，不进 Markdown 表格）：**

10. `playwright_strategy`: `extract` | `defer` | `ai-only`
    - `extract`：稳定的确定性流程（表单校验、状态流转、列表筛选）—— 第一遍跑通即提炼
    - `defer`：业务正确但 UI 仍在迭代 / 文案常变 —— 暂缓，下版本再判断
    - `ai-only`：依赖主观判断（布局合理性、提示文案是否清晰、视觉一致性）—— 永远不提炼
11. `auth`: 字符串。可取 `test/.skill-config.yaml` 中 `auth_modes` 的任一 key（项目自定义角色，如 `customer`、`seller`、`admin`），或保留字 `none`（匿名）/ `fresh`（每例独立 incognito context，常用于登录、注册类用例）
12. `tags`: 字符串数组，用于报告聚合

## 8. 输出表格（Markdown）

| 用例ID | 用例名称 | 用例类型 | 所属模块 | 前置条件 | 步骤描述 | 预期结果 | 标签 | 优先级 |
|---|---|---|---|---|---|---|---|---|

`playwright_strategy` / `auth` 不进表，仅存 meta.json（避免污染人类可读的用例文档）。

## 9. 设计原则（必须遵守）

1. 覆盖优先级：异常 > 边界 > 功能
2. 高风险业务优先（支付、资金、状态流转）
3. 每个输入参数至少覆盖：1 正常值 / 1 边界值 / 1 异常值
4. 所有预期结果必须可验证（「页面正常」「提示合理」不接受）
5. 用例应可复用、可扩展

## 10. 每批用例的总结输出

每次生成完用例必须自我评估：

1. 覆盖度
   - 等价类是否 100%
   - 边界值是否 ≥ 90%
   - 异常场景是否 ≥ 80%
   - 是否使用 Pairwise
2. 风险与建议
   - 是否存在未覆盖风险点
   - 是否需要补充组合 / 特殊场景
   - PRD 是否存在歧义或设计缺陷（如有，明确指出）

把这份自评写入 `test/{version}/test-outline.md` 的「设计自评」章节。
